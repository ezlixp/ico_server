import { API_VERSION } from "../../src/config";

describe(`POST /api/${API_VERSION}/auth/token`, () => {
    it.todo("test getting admin token");
    it.todo("test authorization code skipping discord api calls");
});
